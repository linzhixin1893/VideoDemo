<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="setting_animation">
        <item name="android:windowEnterAnimation">@anim/left_in</item>
        <!-- 指定显示的动画xml  -->
        <item name="android:windowExitAnimation">@anim/left_out</item>
        <!-- 指定消失的动画xml  -->
    </style>

    <style name="setting_llyt_style">
        <item name="android:layout_height">80dp</item>
        <item name="android:layout_width">172dp</item>
        <item name="android:background">@drawable/setting_selector</item>
        <item name="android:gravity">center|left</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingTop">8dp</item>
       <!-- <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>-->
        <item name="android:orientation">horizontal</item>
    </style>
    <style name="setting_text_style">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/setting_text_item_size</item>
        <item name="android:radius">2dp</item>
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:focusable">false</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>

    <style name="setting_item_image_style">
        <item name="android:layout_height">@dimen/setting_item_size</item>
        <item name="android:layout_width">@dimen/setting_item_size</item>
        <item name="android:focusable">false</item>
        <item name="android:focusableInTouchMode">false</item>
    </style>

    <style name="progress_style">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/seek_layer_list1</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">20dip</item>
        <item name="android:maxHeight">20dip</item>
        <item name="android:mirrorForRtl">true</item>
    </style>
</resources>